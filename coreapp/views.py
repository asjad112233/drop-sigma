# NOTE: This `coreapp` Django app is a leftover from the original project
# scaffolding. All real views live in `core.views` (the project's primary
# settings/urls module). Keeping this file empty so the app stays registered
# without confusion; do not add new views here — use `core.views`.
